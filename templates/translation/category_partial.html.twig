<h4>Categorieën</h4>
<div class="row mb-3">
    {% for set in categories|batch(categories|length/3) %}
        <div class="col-md-4">
            <ul class="no-list">
                {% for cat in categories %}
                    <li>
                        <a href="">
                            {{ cat.name|capitalize }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endfor %}
</div>